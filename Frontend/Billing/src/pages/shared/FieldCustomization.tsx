import React from "react";

type FieldCustomizationProps = {
  onClose?: () => void;
  featureType?: string;
};

export default function FieldCustomization(_props: FieldCustomizationProps) {
  return null;
}
